import React from 'react';
import { ScrollView } from 'react-native';

import { Cardh } from '../../components';

const UHistoryscreen = () => {
    return (
        <ScrollView>
            <Cardh />
        </ScrollView>
    );
}

export default UHistoryscreen;
